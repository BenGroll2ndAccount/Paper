from paper.lib import *

wallpapersystem = Paper(
    driver = DisplaySimDriver(
        layout=DisplayLayout.simple(
            width = 300,
            height = 100,
            startwidget = 1
        )
        ),
    
    )
  
